- name: Demo on Loops
  hosts: localhost
  connection: local
  tasks:
    - name: Demo on Loops
      ansible.builtin.debug:
        msg: Fruit name - {{ item }}
      loop:
        - apple
        - mango

